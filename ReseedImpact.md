# 重播种机制如何解释每一个已观察到的现象

真实抓包已经确认（[README.md](README.md) §2.2-2.3、[evidence/bUseTurnSwitchValidation.md](evidence/bUseTurnSwitchValidation.md)、[evidence/action_id-real-capture-sequence.md](evidence/action_id-real-capture-sequence.md)、[evidence/local-vs-confirmed-action-id.md](evidence/local-vs-confirmed-action-id.md)）：`bUseTurnSwitchValidation` 在真实对局里是开启的，`cardsRandomStream` 每次创建/确认动作都会重播种为 `match_id + action_id * 19390`。**注意**：反编译进一步确认，真正喂给这个重播种公式的 `CurrentActionId` 字段，跟提交请求 JSON 里那个已经实测确认"只数我方自己动作、从 1 连续递增"的 `action_id` 字段（`Counter3228`），是原生代码里两个不同的字段——完整区分见 [evidence/two-distinct-action-id-counters.md](evidence/two-distinct-action-id-counters.md)。本文以下用"本地计数器"统一指代喂给重播种公式的那个变量。

**社区的操作计数方法论（完整、权威版本，直接来自社区测试说明，2026-08-25 记录）**：计算所得的操作次数是指两次预报之间的操作次数；**双方操作都计入**（对手的操作也算数，只有"结束回合"这个动作例外——只有己方结束回合才计入，对手结束回合不算）；操作次数**跨回合依然生效**（不是每回合归零重新数）。不同类型的操作权重不同，按"滑动次数"（下称"单位"）计：

| 单位数 | 操作类型示例 |
|---|---|
| 1 | 部署、行动（移动到前线等）、指令（打出 order 类卡）、反制、结束回合（仅己方） |
| 2 | 开发、观星、毁坏、好高骛远（等具体卡牌效果） |
| 3 | 预报单位/预报指令（触发天气预报）、英国太平洋舰队、贝克街分队 |

**这条社区方法论直接推翻了本节此前"本地计数器只数我方自己动作、不受对手影响"的表述**——"双方操作都计入"这一条经社区大量实测确认，跟本报告 [evidence/CurrentActionId-increment-logic.md](evidence/CurrentActionId-increment-logic.md) 反编译 `GetNextAction_Impl` 得到的结论（`CurrentActionId` 由回放合并动作日志的状态机推进，不区分动作归属方）完全吻合——这是反编译结论第一次得到独立于本报告的、大规模社区实测数据的交叉验证，两条完全独立的证据链（静态反编译 vs 社区黑盒测试）指向同一个结论，不再是"哪个更可能对"的问题。本文之前"本地计数器只在玩家自己创建新动作时递增，完全不受对手影响"这一表述是错的，已在下方 §2/§3 更正。

## 1. "1 次预报 = 3 个单位"——现在有了社区确认的精确定义，且跟真实抓包数据大致吻合

按上表，"预报单位/预报指令"本身就是明确列出的 3 单位操作——这不是本报告早期从 `CreateAction_Init` 调用链数出来的猜测，而是社区测试直接给出的权重。用 [evidence/live-match-forecast-validation.md](evidence/live-match-forecast-validation.md) 记录的真实数据核对：第二局对局里两次预报之间夹了一次"行动"（J2F 鸭式移到前线，1 单位）和一次"部署"（第 44 特混舰队，1 单位），加上第二次预报自身的 3 单位，合计 `1+1+3=5` 个社区口径的"单位"；真实观测到的原始 `CurrentActionId` 差值是 8 或 11。`8/5=1.6`、`11/5=2.2`，粗略落在"每个社区单位对应约 2 个原始 `CurrentActionId` 增量"这个数量级附近，不是精确吻合（可能是"部署"类操作在这次具体效果里实际产生了不止 1 个原始增量，或者伤害结算这类子效果也各自贡献了增量），但方向和数量级是一致的，不是矛盾——比本节此前"完全没有数据支持"的结论进了一大步。

## 2. 为什么 6K 在"连续两次预报、中间不做别的事"时看起来不变，而 2K/4K 会变——社区口径下的精确版本

**社区测试说明进一步给出了关键的周期数据，直接可用**：2K、4K 天气都是**18 个（社区单位）操作一循环**，6K 天气是**3 个（社区单位）操作一循环**；计算器给出的是最简操作数，实际按这个循环数的整数倍加上去（`+18x` 或 `+3x`）依然能得到目标天气。这跟 [SpyRing.md](SpyRing.md) §3 用已确认 LCG 常数独立推导出的"间谍组织每步固定后退约 `0.326` 格、主流游程长度 `≈3`"是同一类结构——2K/4K 的 18 单位周期、6K 的 3 单位周期，都是社区口径下"多少个单位操作后，展示结果的模式会重复"这一现象的直接量化。"连续两次预报、中间不做别的事，6K 经常看起来不变、2K/4K 会变"这个现象，现在有了社区给出的精确周期数字支撑：6K 的循环短（3），日常操作量很容易凑成 3 的倍数、显得"总是不变"；2K/4K 循环长得多（18），同样的操作量通常凑不成 18 的倍数，所以更容易表现为"会变"。具体到"一次预报换算成几个社区单位"的问题已在 §1 解决（预报本身就是 3 单位）；至于社区单位换算成原始 `CurrentActionId` 增量的精确比例系数，仍需要更多干净的受控数据点才能彻底钉死，是本报告下一步可以继续做的具体工作，不是泛泛的不确定性。

## 2. 为什么会存在这个漏洞：确定性重播种 + 玩家可自行控制的本地计数器

三点已确认事实（分别在 README.md §1/§2.2/§2.3 和对应证据文件里有完整推导/抓包证据），合在一起构成了漏洞的完整因果链：

1. `cardsRandomStream` 是公开算法的确定性 `FRandomStream`（线性同余生成器），种子完全决定输出序列，没有任何硬件熵源或不可预测输入。
2. 玩家提交动作前看到的所有预览结果（天气预报三选一、间谍组织实际抽到哪张卡等），用的种子是 `match_id + 本地计数器 * 19390`——`match_id` 在对局引导数据里就是明文可见的，本地计数器按 §0 表格列出的权重，随双方任何一方的操作推进（结束回合例外，只有己方计入）。
3. 玩家虽然不能独占这个计数器（对手的操作也会推进它），但可以完全掌握**自己这一侧**能贡献多少：每多做一次任何计入的操作（哪怕是不花费资源、马上撤销的操作，比如激活一次反制立刻取消——按社区确认，激活反制计一次操作，取消反制本身不计），这个本地计数器就会按对应权重推进，进而让种子按已知公式前进相应步数；只要能大致预判对手在这段时间内会做多少操作（或者干脆在对手行动间隙、自己回合内操作），仍然可以把计数器精确推到目标值。

**这就是漏洞的本质**：预览结果不是"随机"给玩家看的，而是"玩家自己已经做过多少次操作"这个自己完全掌握的数字，经过一个公开、确定性的公式算出来的。只要有人像 NZ33 一样，把"本地计数器取值 → 展示结果"这张映射表实测出来，就能反过来推导："我现在的计数器状态对应哪个结果，想要的结果需要计数器走到哪一步，那我只需要再做相应次数不花费用的空操作（比如反复激活/取消一次不实际使用的反制），就能把预览结果精确调整成我想要的"——这正是社区描述的"挂反制→取消反制，不耗费用地控制天气"打法的完整原理，也是官方公告里承认"存在可以被特定操作序列控制"的这个游戏机制问题的根源。

## 3. "跨越多次操作的可查表规律"（Weather.md 的代价表、SpyRing.md 的环形游走）

驱动预报/间谍组织等预提交阶段就能看到结果的效果的，是本地计数器（`CurrentActionId`）。§0 已经更正：这个计数器**不是只数己方动作**——社区测试方法论明确"双方操作都计入"，独立印证了反编译 `GetNextAction_Impl` 得到的"回放合并动作日志、不区分归属方"这一结论。所以社区计算天气/间谍组织规律时，数的是"从某个已知基准点起，双方一共做了多少（加权后的）操作"，不是"我自己做了多少操作"——早期版本"KARDS 是回合制、对方不会插入新动作，所以只数自己回合内的操作也对得上"这个论证虽然在"只看自己单个回合内"这个特殊场景下不会出错（因为那种场景下双方计入和只算自己确实是一回事），但作为通用解释是不必要地绕了弯路：真正的机制本来就是双方都计入，不需要局限于"自己回合内"这个前提。Weather.md §4 记录的社区规律里，"指令数大于 3 时 A/B 两档编号同时升一，持续到第 4 次预报"这种"以操作次数分段、超过阈值后行为整体切换"的现象，本质上就是在描述这个（双方合计、加权后的）本地计数器的某种函数。

## 4. 对局类型覆盖范围

`bUseTurnSwitchValidation` 是服务端随每场对局引导数据下发的值（README.md §2.2 已用 `xrefs_to_field` 排除了客户端硬编码赋值的可能），不是编译进客户端二进制的常量。本报告直接掌握的抓包只有 `match_type: "training"`（人机单机局）两份，均确认该值为 `true`；**PvP 天梯对局同样启用这个开关，已由持有账号、能直接在天梯对局中核实的本项目所有者确认**——也就是说，重播种机制在 PvP 天梯对局下同样是启用状态，跟 `training` 对局一致。因此本报告 §1-3 的全部结论覆盖 `training` 和 PvP 天梯两类对局；唯一仍未覆盖、且没有已知渠道核实的是锦标赛（tournament）等其他赛制，是否有独立于天梯的配置仍是未知。

## 5. 另见

- [README.md](README.md) — 完整机制交叉验证。
- [Weather.md](Weather.md) — 天气规律与 NZ33 确定性代价表。
- [SpyRing.md](SpyRing.md) — 间谍组织环形规律与已确认的完整周期。
- [evidence/bUseTurnSwitchValidation.md](evidence/bUseTurnSwitchValidation.md) — 该开关的证据来源。
- [evidence/action_id-real-capture-sequence.md](evidence/action_id-real-capture-sequence.md) — 服务端权威 `action_id` 真实取值规律。
- [evidence/local-vs-confirmed-action-id.md](evidence/local-vs-confirmed-action-id.md) — 本地投机计数器 vs 服务端确认编号，两套编号的真实差异（本文档结论的核心证据来源）。
