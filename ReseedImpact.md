# 如果 `bUseTurnSwitchValidation` 是开启的：重播种机制会不会让这些规律失效？

Weather.md 和 SpyRing.md 里给出的"结果可预测/可查表"解释，都建立在一个前提上：`bUseTurnSwitchValidation == false`，`cardsRandomStream` 整场只播种一次，此后所有随机效果连续消费同一条序列（详见 README.md §2.2、evidence/bUseTurnSwitchValidation.md）。这个前提**目前只在代码层面证实"存在这个分支、能被跳过"，没有独立证实"实战里到底是不是走的这个分支"**——本文分析另一种情况：如果这个开关实际是**开启**的，前面归纳出的规律还成不成立，以及这对判断"社区的实测到底是在哪种模式下做出来的"意味着什么。

## 1. 开启后的重播种公式回顾

```cpp
// README.md §2.2，BP_OnlineMatch.cpp:24520-24536
private void SetRandomStreamWithActionID(int inputActionID) {
    if (!GetMatchController->bUseTurnSwitchValidation)
        return;
    int seed = Match.match_id + inputActionID * 19390;
    cardFunctions->SetRandomStreamByMatchID(seed);
}
```

开启时，**每一次**创建/确认一个动作，都会把 `cardsRandomStream` 重新播种为 `match_id + action_id * 19390`。触发点有两处（README §2.3）：本地投机创建动作时用本地预测的 `GetCurrentActionID()`，服务端确认动作后再用真实的 `action_id` 重播一次。

## 2. 关键结论：开启重播种，并不会让"确定性"消失——但"预测"这件事本身没有变简单

**这一节最初的版本高估了 `action_id` 的可预测性，已根据 IDA 实测证据修正，见下方"更正"小节。**

直觉上可能会觉得"既然每次都重新播种，序列就被打乱了，规律应该消失"。这个直觉里"规律会消失"的部分是错的，但"预测会变简单"的部分也是错的——准确的结论是：

- `match_id` 在对局开始时就已固定，整场不变，这点没有争议。
- 重播种用的种子公式（`match_id + action_id*19390`）本身没有任何随机成分——只要知道 `match_id` 和这一次触发时 `action_id` 的确切取值，重播种后的结果就是完全确定、可以离线算出来的。这跟"关闭"模式下"结果由累计消费次数唯一决定"是同一类论证，**没有变得不确定**。
- 但"知道 `action_id` 的确切取值"本身不是一件轻松的事——见下方更正。

所以准确的表述是：开启 `bUseTurnSwitchValidation` 不会把"确定性伪随机"变成"不可预测"（种子公式仍然是纯函数，没有引入任何新的随机源），但也不能简单地说这样"更容易预测"——真正决定预测成本的，是复现 `action_id` 取值本身需要多少工作量，而这个工作量并不比"关闭"模式下统计累计消费次数更低。

### 更正（回应用户质疑）："`action_id` 是简单单调递增计数器"这个说法不成立

最初这里写的是"`action_id` 是一个从 0 开始、按对局进程单调递增的小整数计数器……客户端自己就知道下一次会传入什么值"，暗示只要知道对局进行到哪一步就能顺手推算出来。用户当场指出这站不住脚——实战抓包也需要专门写脚本解析才能拿到完整动作列表，不是随口就能数出来的。

顺着 IDA 查了 `GetCurrentActionID()` 背后的真实字段 `CurrentActionId`（native `AMatchControllerV2+0xC98`）是怎么自增的，结果证实用户是对的：它不是简单的 `+1`，而是由 `MatchControllerV2_GetNextAction_Impl`（一个很大的、会递归调用自身的动作日志/回合状态机）根据"这个动作是不是 `XActionStartOfTurn`/`XActionEndOfTurn`/`XActionMulligan`""这个动作是不是重复的""这个动作是不是本方发出的"等多个条件，在不同分支里分别递增，还会在特定条件下（本地已知动作用尽）整体重置为 `1`。完整证据见 [evidence/CurrentActionId-increment-logic.md](evidence/CurrentActionId-increment-logic.md)。

也就是说：要提前知道某次预报/间谍组织触发时的确切 `action_id`，需要在本地完整复现这套状态机、对着真实的动作日志逐条重放——**这跟解析抓包、写脚本还原完整 action 列表几乎是同一件事，工作量并不比"关闭"模式下统计"这是全场第几次消费流"更低**。原先"客户端自己就知道下一次会传入什么值"这个说法，只在最字面的意义上成立（本地代码调用 `GetCurrentActionID()` 确实能拿到当前值——但那是在事件发生的当下读到的，不是"提前推算"），不构成"这套机制更容易被预测/利用"的证据。第 4 节的开放问题结论不受影响：两类现象仍然分别更自然地指向两种不同模式，需要专门的对照实验来区分，这一点没有改变。

换一个角度说，两种模式下"建可查表"所需要的自变量是不同的：

- **关闭模式**："这是本场第几次调用 `RandomIntFromRangeWithStream`（对整场累计计数）"。
- **开启模式**："接下来这个动作的 `action_id` 是多少，而在这个新种子重播种之后，这是本次动作里第几次调用 `RandomIntFromRangeWithStream`（对单个动作内部计数，通常是 0/1/2 这种小数字）"。

两者都需要"完整、逐条地"知道对局到目前为止发生过的动作序列——区别只在于关闭模式数的是"随机流被消费的次数"，开启模式数的是"`action_id` 按第 2 节确认的状态机规则推进到了第几"，**两者的信息获取成本是同一量级的**，不存在谁比谁"更容易离线打表"。

## 3. 对已发现规律的具体影响，分两类讨论

### 3.1 "同一次预报反复打开，结果不变"——两种模式下都成立，原因不同

- **关闭时**：`_forecastOptions` 缓存 + 没有任何重播种，"重复观察不消耗随机数"，天然不变。
- **开启时**：只要没有创建新动作（`action_id` 没有推进），`cardsRandomStream` 也不会被重新播种——单纯重复打开预报面板本来就不创建新动作，所以这一条现象在两种模式下都会观察到，**不能用来区分到底是哪种模式**。

### 3.2 "跨越多次操作的可查表规律"（Weather.md §4 的 A/B/C 编号表、SpyRing.md 的操作间隔猜想）——两种模式下含义不同

这是能区分两种模式的关键点：

- **如果关闭**：规律来自"整场共享同一条连续序列"，正确的自变量是"全场累计消费次数"。社区如果是按"这一局里我一共做了多少次可能消耗随机数的操作"来对齐数据、发现规律，那这类归纳方式本身就已经隐含假设了"关闭"这个前提，且规律成立本身就是关闭模式的证据。
- **如果开启**：每次创建新动作都会重播种，"全场累计消费次数"这个自变量就**不再适用**——同一回合内如果连续做了两个动作、且这两个动作之间 `action_id` 正确递增，理论上会重播种成两个不同的种子,规律不会以"全场第 N 次消费"的形式呈现;但规律仍然可能存在，只是要换成以"`action_id` 的差值/奇偶/序列位置"为自变量重新归纳——这正是社区目前的数据没有明确排除的一种可能性。

Weather.md §4 记录的社区规律里,有一条特别值得注意:"如果开预报之前本回合已用指令数**大于 3**,AB 两档编号会同时'升一',且这个状态会持续到本回合第 4 次预报"——这种"以本回合内操作次数分段、超过某个阈值后行为整体切换"的现象,形式上更像是在描述"`action_id` 的某种函数",而不是单纯的"连续消费同一条序列",不能排除这本身就是开启模式下、重播种以 `action_id` 为自变量呈现出来的规律,只是社区目前是用"指令数"这个更直观的代理变量去描述它。

### 3.3 README §2.3 提到的"同回合内第二次开发结果一样"

这条现象,在两种模式下都有各自独立的解释:

- **关闭时**:两次开发就是连续消费同一条流的两段,如果两次 `GetChooseSpawnCards` 中间没有发生别的消费,理论上结果确实会不同(除非流位置恰好绕回),这条现象反而不那么自然。
- **开启时**:如果本地投机计数器 `GetCurrentActionID()` 在两次开发之间**没有正确自增**(比如两次触发之间没有走完一次完整的"创建→提交→确认"闭环),两次重播种会用**同一个** `action_id`,种子完全相同,三个抽签结果自然完全一样——这是 README §2.3 原本就点出的解释,而且这个解释本身就已经预设了"开启"这个前提(只有开启时,`action_id` 才会参与播种;关闭时压根不会因为 `action_id` 有没有正确自增而受影响)。

## 4. 结论:目前不知道社区的发现是在哪种模式下得到的,而且这非常重要——需要用一次专门设计的对照实验来区分

把第 3 节摊开看,会发现一个尴尬的事实:**本报告和社区目前收集到的现象,分别更自然地指向两种互相矛盾的模式**——"全场累计消费次数可查表"类现象(6K/4K 规律、间谍组织环形游走、操作间隔猜想)更自然地指向**关闭**;"同回合内重复开发结果相同"类现象更自然地指向**开启**(且是"投机计数器未正确自增"这个特定 bug 触发的)。这两类现象不能同时用"同一种模式、同一套自变量"完整解释,说明:

- 要么实际游戏里 `bUseTurnSwitchValidation` 的值本身**不是全局固定的**,不同对局类型(单机 AI / 天梯 / 锦标赛)可能取值不同,社区不同视频、不同玩家的实测可能是在不同模式下做的,规律的具体形式也会不一样;
- 要么其中一类现象的归因是错的,需要重新审视。

**建议的对照实验**(有条件复现的话,比继续用 IDA 死抠反汇编更快得出结论):在同一局内,分别记录"全场累计消费次数"和"当前动作的 `action_id`"这两个自变量,看规律到底是跟哪一个对齐——如果是关闭模式,把数据按 action_id 重排后规律应该被打乱;如果是开启模式,按累计消费次数对齐的规律应该在跨动作边界处断裂、而按 action_id 对齐后应该重新变得整齐。这也是本报告目前唯一一个"社区已经有足够数据、但还没有人专门去做"的验证点。

## 5. 另见

- [README.md](README.md) — 完整机制交叉验证。
- [Weather.md](Weather.md) — 天气规律与社区 A/B/C 编号表。
- [SpyRing.md](SpyRing.md) — 间谍组织环形规律与操作间隔猜想。
- [evidence/bUseTurnSwitchValidation.md](evidence/bUseTurnSwitchValidation.md) — 该开关的证据来源与强度声明。
