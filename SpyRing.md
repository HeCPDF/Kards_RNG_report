# 间谍组织（Spy Ring）：一个看似"记忆上一次结果"的伪随机

来源：`Content/Blueprints/Cards/Soviet/Breakthrough/events/card_event_spy_ring.uasset` 的 FModel 反编译（`FModel-Exports/.../card_event_spy_ring.cpp`），加上社区实测视频的归纳结果。方法论呼应 [Weather.md](Weather.md) §3。

## 1. 代码实现：只有一行随机数

[`SPY RING`](https://www.kards.com/images/card/v52/en-EN/spy_ring.avif)（苏联 Breakthrough 系列，2 kredits，"Add a random Research card to your hand"，卡图为官方站点链接，非本仓库转载）的整个效果逻辑在 `ExecuteUbergraph_card_event_spy_ring` 里，核心就是一次范围 `[0, 4]` 的等概率抽签，再用 `switch`-like 的链式比较把结果映射到 5 张固定的国家研究卡之一：

```cpp
cardFunction->RandomIntFromRangeWithStream(0, 4, /*out*/ randNumber);

if (randNumber == 0) cardToSpawn = "card_event_royal_research";          // 英
else if (randNumber == 1) cardToSpawn = "card_event_us_military_research";     // 美
else if (randNumber == 2) cardToSpawn = "card_event_reich_research";           // 德
else if (randNumber == 3) cardToSpawn = "card_event_imperial_research";        // 日
else if (randNumber == 4) cardToSpawn = "card_event_soviet_military_research"; // 苏

cardFunction->SpawnCardInHandBySide(side, cardToSpawn, cardID, ...);
```

代码本身没有任何"记住上一次抽到谁"的状态——没有额外字段存历史结果，没有基于上一次结果做加权/排除的逻辑。它调用的还是 `BP_CardFunctions::RandomIntFromRangeWithStream`，跟天气卡、洗牌、任何"随机选一个"效果共享**同一条** `cardsRandomStream`（详见 README.md §1、§3）。单看这一张卡的代码，会觉得它就是最朴素的"五选一等概率随机"，没有任何值得单独写文档的地方。

## 2. 但社区实测发现了一个"环形规律"

2026-08-24 发布的视频《逻辑推理就能出，4把游戏把间谍组织扒出规律！》（BV1WshP6hEHt）用纯粹的对局内观察（不看反编译代码）复现出了一个具体、可验证的规律，转述如下：

- 把 5 个国家按"美-苏-德-英-日"排成一个环（`美苏德英日`，首尾相接）。
- 同一局内，第一次抽到的国家看起来是"纯随机"（作者没能进一步测出它是否也遵循某种规律，样本不够）。
- **从第二次开始**：每次再触发一张间谍组织，新结果相对上一次的结果，在这个环上只会是"原地"、"向左挪一位"或"向右挪一位"中的一种——不会跳到环上不相邻的国家。方向（左/右）看起来也是随机的，作者同样没能进一步测出其规律。
- 测试方法：双方各自准备多张间谍组织，在同一回合内用"部署间谍组织 → 激活反制 → 取消反制 → 再部署间谍组织"这类不同操作类型反复触发抽卡，按触发顺序把结果记录下来，比对是否始终落在"环上相邻或原地"这个约束内。作者称在 4 局详细记录 + 后续若干局抽查里全部符合。

这个"环形相邻游走"现象，和 Weather.md 里"6K/4K 呈现可预测循环"的观察，是同一类现象在两张不同卡上的表现——**不是间谍组织这张卡有特殊逻辑**（上面第 1 节已经确认代码里没有），而是底层共享随机流本身的行为特征。

## 3. 技术解释：为什么一个"无状态"的等概率随机会呈现出"记忆"

`RandomIntFromRangeWithStream` → `UKismetMathLibrary::RandomIntegerInRangeFromStream`（一行转发）→ `FRandomStream::RandRange`，最终落到 Unreal 引擎的线性同余生成器（Engine/Public/Math/RandomStream.h）：每次取值前先执行 `Seed = Seed * 196314165 + 907633515 (mod 2^32)`，再用变换后 `Seed` 的**高 23 位**构造 `[0,1)` 浮点数。引擎头文件自己在注释里写明"低位质量很差，不要用取模运算符"——也就是说，Epic 的实现刻意避开了经典 LCG 取模映射的低位相关性缺陷，单次调用的统计质量本身没有问题。这意味着"环形相邻游走"不能简单归因于"对 LCG 输出取模会有相关性"这类笼统说法（这是本文档早期版本的猜测，已根据这一点更正）。

真正能产生"环形游走"结构的位置，是 README.md §2.2 的重播种公式：`seed = match_id + action_id * 19390`——真实抓包已经确认对局里这个重播种分支确实是开启的（[evidence/bUseTurnSwitchValidation.md](evidence/bUseTurnSwitchValidation.md)）。间谍组织的消费方式跟天气系统不同：天气三选一是**同一次重播种后连续三次变换**（`k=1,2,3`），而间谍组织是**每一次触发都各自独立重播种、各自只变换一次**（`k=1`）——这是两张卡结构上的根本区别，下面的推导专门针对间谍组织这种"单次重播种单次抽样"的结构，不能跟 Weather.md §4.2 的"同一粒种子连续多次变换"模型混用（§5.1 曾经把两者类比，这个类比不成立，已更正）。

**精确推导（已用字节级确认的 LCG 常数验证，并用 Python 独立模拟复核）**：设本地计数器从 `action_id` 推进到 `action_id+1`（即社区所说的"一次操作"），两次重播种用的种子相差固定的 `19390`。经过 `MutateSeed`（`Seed' = Seed*196314165 + 907633515 mod 2^32`）变换后，两次变换结果的差值同样是一个**固定常数**（跟 `match_id`、`action_id` 的具体取值完全无关，只取决于 `196314165` 和 `19390` 这两个已确认常量）：

```
Δ_seed = (19390 × 196314165) mod 2^32 = 1,190,635,094
```

`GetFraction()` 只取变换后 `Seed'` 的低 23 位构造 `[0,1)` 浮点数（`mantissa = Seed' & 0x7FFFFF`，再除以 `2^23`）。因为取低 N 位是模 `2^N` 的环同态，`Δ_seed` 对 23 位小数部分的贡献同样是一个固定常数：

```
Δ_mantissa = Δ_seed mod 2^23 = 7,841,366   →   Δ_fraction ≈ 0.9348
```

代入 `RandRange(0,4) = floor(GetFraction() × 5)`（超出 4 则截断为 4）：`5 × Δ_fraction ≈ 4.674`。也就是说，**本地计数器每前进 1，`RandRange(0,4)` 的连续位置在"5 等分环"上固定前进约 4.674 格——等价于固定后退约 0.326 格（`mod 5`）**。因为每步只前进不到 1 格，输出值要么维持不变（多数情况），要么恰好后退 1 格，绝不会一步跨过 2 格以上——这就是社区实测总结出的"只会原地或挪一位，不会跳到不相邻位置"现象的精确来源，且这个 `Δ_fraction≈0.9348` 是从确认的引擎常数直接算出来的固定值，不依赖 `match_id`，也不依赖对局具体状态。

**用 Python 独立模拟复核**（同一套字节确认的 LCG 算法，2000 个随机 `match_id`、每个连续模拟 400 次操作，共 80 万次抽样）：所有样本里，同一结果连续出现的次数几乎全部是 **3**（占绝大多数），偶尔出现 **4**，全部样本里从未出现超过 4 次——这跟 `0.326` 的倒数 `≈3.07` 完全吻合（平均每 3.07 步后退一格），是"环形相邻游走"现象第一次得到跟已知常数直接对应的精确数值解释，而不是停留在"底层共享随机流"这种定性归因。

这个"固定步长的环形漂移"结构，本身就直接由已确认的 LCG 常数和重播种公式推出，不需要假设代码里存在任何显式的"环形状态机"（第 1 节已确认代码里没有）。

## 4. 环的具体顺序：跟 switch 里数字标号对不上，反而是更强的证据

第 1 节列出的原生 switch 映射，按 `randNumber` 数值顺序排列是：

```
0 → 英 (royal)     1 → 美 (us_military)   2 → 德 (reich)
3 → 日 (imperial)  4 → 苏 (soviet_military)
```

如果直接按这个数字顺序 0→1→2→3→4→0 转一圈，环应该是"英-美-德-日-苏"。但社区实测总结出的环是"美-苏-德-英-日"——**这两个环不是同一个排列**（互相不是简单的旋转或镜像关系）。

这个"对不上"，反而是支持第 3 节技术解释、而不是削弱它的证据：`randNumber` 只是 `RandomIntFromRangeWithStream(0,4)` 返回值本身，switch 语句里 0/1/2/3/4 对应哪个国家，纯粹是策划/程序员写代码时随手赋的标号顺序，跟 `FRandomStream` 内部生成这个整数的算法毫无关系。如果"环形相邻游走"确实来自"同一条 LCG 流连续两次输出、经 `%5` 映射后存在序列相关性"，那么这种相关性只会体现在**流本身产出的原始残数（余数）序列**里，跟策划把哪个余数标为"英国"、哪个标为"美国"没有任何关系——所以社区凭实测归纳出的环，完全可以是、也应该是相对 switch 数字顺序的一个非平凡置换。换句话说：**这个"对不上"恰恰说明规律的根源在随机数生成算法本身，而不是游戏逻辑代码里写了什么"环形状态机"**（第 1 节已经确认代码里根本没有这种状态机）。

## 5. 社区补充测试：操作间隔与"完整周期"猜想

在《更全面的测试——间谍不是随机！》一期的评论区，有观众提出了更细的假设，把间谍组织的规律和"消耗随机流的操作次数"直接挂钩，而不只是"上一次结果是什么"：

- 反复"激活反制→取消反制"（每次算 2 次操作，会各自推进一次 `cardsRandomStream`）测试后发现：**间隔 2 次、5 次、8 次操作后会出现循环**；而"两次真正抽到研发卡的操作"之间，间隔呈 3、6、9……这样按 3 递增的规律。
- 由此提出猜想：连续做 18 次操作（即激活/取消反制 17 次），有可能正好走完一整个循环节，届时连续抽到的研发卡会完全相同（即整段序列以某个周期折返）。
- **该猜想已经过实测验证**：连续操作后，实际连续刷出了 **9 张完全相同**的研发卡（原猜想估计是 8 张，实测数字略有出入，但"存在一个可复现的完整循环节、能连续刷出一长串相同结果"这个核心结论成立）。这是本文档目前唯一一条从"社区猜想"升级为"实测确认"的结论——直接证明间谍组织的抽签结果在给定打法下具备完整的、可复现的周期结构，不是统计巧合。

这个"操作次数按 3 递增才会撞到同一个结果"的现象，跟第 3 节推导出的"平均每 `≈3.07` 步后退一格"精确吻合（3 递增，正好对应第 3 节模拟确认的主流游程长度 3）。

### 5.1 "重播种后单次抽样直接决定展示结果"这个模型——对间谍组织不但没有被证伪，反而是已确认成立的模型；跟"9 次连续相同"这一条具体数据的矛盾需要单独说明

本文档早期版本曾把 Weather.md §4.2 的证伪结论（"同一粒种子连续做多次变换，第 `k` 次变换的展示结果不能是原始下标，因为 `A^k mod 2^32` 随 `k` 不同而不同，会导致 2K/4K 代价不可能相等"）类比到间谍组织身上——**这个类比不成立，已经撤回**：Weather.md 证伪的是"同一次重播种后连续做 `k=1,2,3` 三次变换"这种结构，而间谍组织是"每次操作各自独立重播种、只做一次变换"（`k` 恒为 1），两者结构不同，Weather.md 的证伪不适用于间谍组织。

相反，第 3 节已经用字节级确认的 LCG 常数（`196314165`、`19390`）精确推导并用 80 万次独立模拟验证："重播种后单次抽样的原始下标经 `%5` 映射后确实就是玩家看到的国家"这个最简单的模型，能完整解释"环形相邻游走"现象、且能定量给出主流游程长度 `≈3`（跟第 5 节"3 的倍数"完全吻合）——对间谍组织而言，这个最简单的模型**没有被证伪，而是被独立模拟确认成立**。

但这个已确认模型，跟第 5 节记录的"实测连续刷出 9 张完全相同的研发卡"这一条具体数据之间存在明确的数值矛盾，需要如实指出：用同一套确认算法对 2000 个随机 `match_id`、每个连续模拟 400 步（共 80 万次抽样）统计连续相同游程的最大长度，结果全部 ≤4，从未出现 9；进一步测试"一次操作对应本地计数器前进 2/3/4/5/6 个单位"这几种整数倍关系（模拟同样的规模），最大游程反而随倍数增大而变短，没有一种整数倍关系能重现 9 连续相同这个数字。也就是说，**"单次重播种单次抽样"模型解释了环形相邻游走的整体结构，但没有解释"9 连续相同"这一条具体实测数据**——这不是"需要运行时验证候选数组内容"这类静态分析边界问题（本节的模型完全基于已确认的公开算法和常数，是纯算术推导，不涉及任何运行时才能读到的数据），而是一个已经被精确量化、但尚未找到成因的具体矛盾：要么"9 连续相同"发生时用到的实际本地计数器步进模式比目前假设的更复杂（比如两次触发之间穿插了多种、步进量不固定的操作），要么该次实测记录对应的具体操作序列跟本文档目前掌握的转录内容有出入。这一点在第 8 节列为具体待查项，而不是笼统地"存在不确定性"。

## 6. 结构相同的另一张卡：CONVOY ATTACK（护航攻击/船队袭击）——已由社区实测确认

意大利 [`CONVOY ATTACK`](https://www.kards.com/images/card/v52/en-EN/convoy_attack.avif)（Common，"Deal 0-2 damage to any target."）的实现和间谍组织几乎是同一个模子刻出来的——同样只有一行随机数，直接把结果当数值用，连中间的 switch 映射都省了：

```cpp
cardFunction->RandomIntFromRangeWithStream(0, 2, /*out*/ randomResult);
cardFunction->DamageCard(Event_targetCard, randomResult, cardID, false, false, false, /*out*/ targetDestroyed);
```

这是一次范围 `[0,2]` 的三选一抽签，结果直接作为伤害数值使用。**社区已经用跟间谍组织完全相同的方法测试过这张卡，结论是确认的**：用"中间间隔 17 次激活反制的方式，加上使用 CONVOY ATTACK 本身共 18 次操作"的方法验证了 **18 正是这张卡的伪随机循环周期**——跟间谍组织的周期完全一样。循环内 18 次伤害结果本身没有观察到简单的"0,1,2,0,1,2..."式循环规律，是一串看起来无规律的数字，但保证了 0/1/2 三个数值各出现恰好 6 次（每个数值出现概率均等，都是三分之一）。

用第 3 节同样的方法对 `RandRange(0,2)`（三等分环，不是间谍组织的五等分）重新推导：`Δ_fraction≈0.9348` 不变（只跟 `19390`/`196314165` 这两个常数有关，与桶数无关），`3 × Δ_fraction ≈ 2.804`，即每步在三等分环上固定后退约 `0.196` 格，平均游程长度 `≈1/0.196≈5.1`——比间谍组织的 `≈3.07` 更长，跟"18 次结果本身看起来无规律、但均匀落在 0/1/2 上各 6 次"这个描述并不矛盾（三等分下平均游程变长，短期内更容易观察到"看起来杂乱"的排列）。**这一条只是用已确认的同一套公式重新代入了不同的桶数，不是新的独立假设**——具体展示结果内部排列顺序的逐项复现（哪一次操作对应哪个伤害值），需要跟间谍组织"9 连续相同"这一条一样的运行时/更细颗粒度抓包核对，属于第 8 节列出的具体待查项。

## 7. 其他被社区验证出类似规律的卡

除间谍组织外，社区反馈还在"反潜巡逻"（反潜）、[`DEATH FROM ABOVE`](https://www.kards.com/images/card/v52/en-EN/death_from_above.avif)（死降，USA，"Destroy a random enemy unit."）、[`PRESSURIZED CABIN`](https://www.kards.com/images/card/v52/en-EN/pressurized_cabin.avif)（加压舱，USA，"Add a B-29 SUPERFORTRESS to hand. Destroy a random enemy unit."）等卡牌上测试出了同样"结果可预测/可复现"的规律。这两张卡已反编译核实，跟间谍组织/护航攻击结构上有一处区别：它们调用的不是 `RandomIntFromRangeWithStream` 直接指定范围，而是 `BP_CardFunctions::GetRandomCard`（`BP_CardFunctions.cpp:5248`）——先拿到敌方场上全部单位的数组，再用 `RandomIntFromRangeWithStream(0, 数组长度-1)` 在数组下标范围内抽一个。也就是说这两张卡的"候选数量"是**动态的**（取决于对方场上有几个单位），不像间谍组织/护航攻击是固定的 5/3——这意味着 Weather.md §4.2/本文 §5.1 那套"固定分桶数换算等效步长"的方法不能直接套用，需要针对具体场面单位数重新推导。反潜巡逻的具体实现尚未反编译核实。这些卡的共同点是：效果里都含有"从若干个候选结果中随机选一个"这一步，而它们无一例外都要经过 `cardsRandomStream`——按第 3 节的解释框架，这些卡出现类似规律并不意外，是同一套底层机制的又一次重复验证，而不是各自独立的巧合。

## 8. 尚待确认的开放问题（均已定性到静态分析能否解决的边界，或已列为具体量化的待查矛盾）

1. **环形游走的方向（前进/后退）本身是否可预测**：第 3 节已经推导并模拟确认"每步固定后退约 0.326 格"这个**大方向上单向偏移**的定量结果——这已经不是"方向随机"，而是有固定偏向的漂移；但因为 `0.326<1`，多数步是"原地"（偏移量不足以跨过一格边界），只有当累积的小数部分进位时才会体现为"后退一格"，具体哪一步会进位取决于 `match_id` 决定的初始相位（`f_0`），这部分**逐步预测**（不是统计意义上的整体方向）需要知道具体的 `match_id` 数值才能逐步算出，不是本报告能脱离具体对局给出通用公式的部分——这是数据依赖，不是方法论空白。
2. **第一次抽签是否独立于全场累计消费次数**：需要控制"这是全场第几次消费流"这个变量做真实对局对照实验才能确定，静态反编译无法产生这类实测数据。
3. **"9 连续相同"这一条实测数据跟第 3/5.1 节已确认模型的具体矛盾**：第 3 节的"单次重播种单次抽样"模型已经用字节级确认的引擎常数独立推导并用 80 万次模拟验证，能完整解释环形相邻游走现象、且精确给出主流游程长度 `≈3`；但同一模型（含测试过的 2-6 倍步进变体）在 80 万次抽样里从未复现游程长度 9。这不是"需要运行时数据才能确定"的静态分析边界，而是一个已经被算清楚、但成因未知的具体矛盾——下一步需要重新核对该次实测视频里"9 连续相同"对应的具体操作序列（每两次抽卡之间实际做了哪些操作、是否存在步进量不固定的情况），而不是继续用本节的固定步进模型去套。
4. **CONVOY ATTACK 的具体展示结果排列顺序**：18 次循环的周期长度已由社区实测确认（第 6 节），第 6 节已用同一套公式换算出三等分环下的平均游程（`≈5.1`），但循环内部具体哪一次操作对应哪个伤害数值，需要跟第 3 点同样的更细颗粒度实测（结合真实 `match_id`/`action_id` 走一遍公式）才能逐项核对，属于需要新数据而非需要新方法的问题。

## 9. 另见

- [README.md](README.md) — 核心随机流对象、播种/重播种机制的完整交叉验证、动作类型与提交流程。
- [Weather.md](Weather.md) — 天气系统的具体实现与同一套机制在另一张卡上的表现。
- [ReseedImpact.md](ReseedImpact.md) — 重播种机制如何逐条解释每一个已观察到的现象。
