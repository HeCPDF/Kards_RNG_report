# 间谍组织（Spy Ring）：一个看似"记忆上一次结果"的伪随机

来源：`Content/Blueprints/Cards/Soviet/Breakthrough/events/card_event_spy_ring.uasset` 的 FModel 反编译（`FModel-Exports/.../card_event_spy_ring.cpp`），结合 IDA/引擎常数推导。方法论呼应 [Weather.md](Weather.md) §3；社区实测数据统一收录于 [社区实测记录](社区实测记录.md) §3，不构成本文主证据，只在第 4 节末尾用于对照。

## 1. 代码实现：只有一行随机数

[`SPY RING`](https://www.kards.com/images/card/v52/en-EN/spy_ring.avif)（苏联 Breakthrough 系列，2 kredits，"Add a random Research card to your hand"，卡图为官方站点链接，非本仓库转载）的整个效果逻辑在 `ExecuteUbergraph_card_event_spy_ring` 里，核心就是一次范围 `[0, 4]` 的等概率抽签，再用 `switch`-like 的链式比较把结果映射到 5 张固定的国家研究卡之一：

```cpp
cardFunction->RandomIntFromRangeWithStream(0, 4, /*out*/ randNumber);

if (randNumber == 0) cardToSpawn = "card_event_royal_research";          // 英
else if (randNumber == 1) cardToSpawn = "card_event_us_military_research";     // 美
else if (randNumber == 2) cardToSpawn = "card_event_reich_research";           // 德
else if (randNumber == 3) cardToSpawn = "card_event_imperial_research";        // 日
else if (randNumber == 4) cardToSpawn = "card_event_soviet_military_research"; // 苏

cardFunction->SpawnCardInHandBySide(side, cardToSpawn, cardID, ...);
```

代码本身没有任何"记住上一次抽到谁"的状态——没有额外字段存历史结果，没有基于上一次结果做加权/排除的逻辑。它调用的还是 `BP_CardFunctions::RandomIntFromRangeWithStream`，跟天气卡、洗牌、任何"随机选一个"效果共享**同一条** `cardsRandomStream`（详见 README.md §1、§3）。单看这一张卡的代码，会觉得它就是最朴素的"五选一等概率随机"，没有任何值得单独写文档的地方。

## 2. 技术解释：为什么一个"无状态"的等概率随机会呈现出"记忆"

`RandomIntFromRangeWithStream` → `UKismetMathLibrary::RandomIntegerInRangeFromStream`（一行转发）→ `FRandomStream::RandRange`，最终落到 Unreal 引擎的线性同余生成器（Engine/Public/Math/RandomStream.h）：每次取值前先执行 `Seed = Seed * 196314165 + 907633515 (mod 2^32)`，再用变换后 `Seed` 的**高 23 位**构造 `[0,1)` 浮点数。引擎头文件自己在注释里写明"低位质量很差，不要用取模运算符"——也就是说，Epic 的实现刻意避开了经典 LCG 取模映射的低位相关性缺陷，单次调用的统计质量本身没有问题；任何"看似记忆"的规律不能简单归因于"对 LCG 输出取模会有相关性"这类笼统说法。

真正能产生规律的位置，是 README.md §2.2 的重播种公式：`seed = match_id + action_id * 19390`——真实抓包已经确认对局里这个重播种分支确实是开启的（[evidence/bUseTurnSwitchValidation.md](evidence/bUseTurnSwitchValidation.md)）。间谍组织的消费方式跟天气系统不同：天气三选一是**同一次重播种后连续三次变换**（`k=1,2,3`），而间谍组织是**每一次触发都各自独立重播种、各自只变换一次**（`k=1`）——这是两张卡结构上的根本区别，下面的推导专门针对间谍组织这种"单次重播种单次抽样"的结构，不能跟 Weather.md §4.2 的"同一粒种子连续多次变换"模型混用。

**精确推导（已用字节级确认的 LCG 常数验证，并用 Python 独立模拟复核）**：设本地计数器从 `action_id` 推进到 `action_id+1`（即本地计数器前进 1 个单位），两次重播种用的种子相差固定的 `19390`。经过 `MutateSeed`（`Seed' = Seed*196314165 + 907633515 mod 2^32`）变换后，两次变换结果的差值同样是一个**固定常数**（跟 `match_id`、`action_id` 的具体取值完全无关，只取决于 `196314165` 和 `19390` 这两个已确认常量）：

```
Δ_seed = (19390 × 196314165) mod 2^32 = 1,190,635,094
```

`GetFraction()` 只取变换后 `Seed'` 的低 23 位构造 `[0,1)` 浮点数（`mantissa = Seed' & 0x7FFFFF`，再除以 `2^23`）。因为取低 N 位是模 `2^N` 的环同态，`Δ_seed` 对 23 位小数部分的贡献同样是一个固定常数：

```
Δ_mantissa = Δ_seed mod 2^23 = 7,841,366   →   Δ_fraction ≈ 0.9348
```

代入 `RandRange(0,4) = floor(GetFraction() × 5)`（超出 4 则截断为 4）：`5 × Δ_fraction ≈ 4.674`。也就是说，**本地计数器每前进 1，`RandRange(0,4)` 的连续位置在"5 等分环"上固定前进约 4.674 格——等价于固定后退约 0.326 格（`mod 5`）**。因为每步只前进不到 1 格，输出值要么维持不变（多数情况），要么恰好后退 1 格，绝不会一步跨过 2 格以上；这个 `Δ_fraction≈0.9348` 是从确认的引擎常数直接算出来的固定值，不依赖 `match_id`，也不依赖对局具体状态。

**用 Python 独立模拟复核，逐个整数确定单次本地计数器步进（`g=1`）时的精确离散步长分布**（20000 次随机 `match_id`/`action_id` 采样）：`raw_step = (RandRange(0,4)_后 - RandRange(0,4)_前) mod 5` 只会取两个值——`0`（67.4%）或 `4`（即 `-1 mod 5`，32.6%），**从未出现过任何其他值**。这不是近似结论，是把已确认的 `Δ_fraction≈0.9348` 代入 `RandRange(0,4)` 离散化规则后的精确算术推论：连续位置每步固定前进 `4.674` 格（不到 5 格），所以离散下标要么保持不变、要么精确前进 4 格（等价于后退 1 格），不可能是别的值。

这意味着**单次本地计数器步进时，原始下标只会停留在原地，或者精确移动到"数值上相邻的下一个"（`v → v-1 mod 5`）**——链式应用这个规则，得到的是 switch 里那五个数字**本来的自然顺序** `0-1-2-3-4-0`（也就是 `英-美-德-日-苏`）。也就是说，**只要严格按每次操作对应本地计数器精确 +1 单位来观察，"环"本身就是 switch 标号的自然数字序，不需要任何额外假设——这一点完全由已确认的 LCG 常数和离散化规则精确推出，不依赖任何本报告之外的数据。**

## 3. 原始下标的离散步长，是本地计数器步进量 `g` 的函数——完整对照表

第 2 节只算了 `g=1`（单次本地计数器步进）这一种情形。同一套推导可以对任意步进量 `g` 重复：把 `Δ_seed(g) = g × 19390 × 196314165 mod 2^32` 代入同样的离散化规则（`GetFraction()` 取低 23 位、`floor(× 5)`），逐个整数测出每个 `g` 对应的离散步长分布（20000 次随机 `match_id`/`action_id` 采样）：

| `g`（本地计数器步进量） | 主导步长（mod 5） | 出现概率 |
|---|---|---|
| 1 | -1（即 4） | 67.4% |
| 3 | -1（即 4） | 97.7% |
| 6 | +3 | 95.9% |
| 9 | +2 | 93.5% |
| 12 | +1 | 91.5% |
| 15 | 0 | 89.6% |

`g` 每增加 3，主导步长精确减少 1（`mod 5`）——这是第 2 节"平均每 `≈3.07` 步后退一格"这个漂移速率的直接推论；且在 `g` 是 3 的倍数时，"哪个具体步长占主导"这件事的确定性显著更高（`g=3,6,9,12,15` 的主导概率均在 90% 以上，明显高于非 3 倍数的 `g`）。这张表本身完全由已确认的 LCG 常数和重播种公式算出，没有用到任何本报告之外的数据。

## 4. 与社区实测的对照

以上第 1-3 节的全部推导只用到已确认的 LCG 常数（`196314165`、`19390`）和重播种公式，没有用到任何社区数据。现在拿第 3 节的表去对照社区独立实测出的规律。

### 4.1 环形相邻游走

社区在 2026-08-24 的视频（BV1WshP6hEHt）里用纯对局内观察复现了一个可验证的规律：把 5 个国家按"美-苏-德-英-日"排成环后，从第二次触发间谍组织起，新结果相对上一次只会是"原地"或"左右相邻"，不会跳到不相邻的国家；第一次结果看起来是"纯随机"。完整转述见 [社区实测记录](社区实测记录.md) §3.1。

这个环（raw 数值序列 `1,4,2,0,3`）精确满足 `环[i] = (1 + 3×i) mod 5`——即"固定跳 3"，跟第 3 节表格里 `g=6` 那一行（主导步长 `+3`，概率 95.9%）完全吻合。这给出一个从代码/常数推导反过来印证社区数据的具体结论：**社区测试间谍组织时，两次被记录为"连续观察"的抽签之间，本地计数器实际步进的量大概率集中在 6 个单位左右（而不是想当然的 1）**——这跟 ReseedImpact.md/[社区实测记录](社区实测记录.md) §1 记录的"预报单位/预报指令 3 单位、指令类 1 单位"等社区权重表是同一类"每个游戏内操作对应多少本地计数器单位"的问题，只是间谍组织场景下具体是哪个数字，这是第一次被推出来。

### 4.2 操作间隔与"完整周期"

社区在《更全面的测试——间谍不是随机！》相关讨论中提出了"反复激活/取消反制、按操作间隔统计循环"的猜想，并实测得到"连续刷出 9 张完全相同研发卡"等数据；完整转述见 [社区实测记录](社区实测记录.md) §3.2。其中"操作次数按 3 递增才会撞到同一个结果"的现象，与第 2 节推导出的"平均每 `≈3.07` 步后退一格"精确吻合（3 递增正好对应主流游程长度 3）。

### 4.3 "9 次连续相同"这一条具体数据的矛盾

第 2/3 节的"单次重播种单次抽样"模型已经用字节级确认的引擎常数独立推导并用大规模模拟验证，能完整解释环形相邻游走现象、精确给出主流游程长度 `≈3`，还能精确推出社区环的具体排列（`g=6` 对应 `+3` 步长）——**这个模型本身没有被证伪，而是被独立模拟确认成立**。

但这个已确认模型，跟 [社区实测记录](社区实测记录.md) §3.2 记录的"实测连续刷出 9 张完全相同的研发卡"这一条具体数据之间存在明确的数值矛盾，需要如实指出：用同一套确认算法对 2000 个随机 `match_id`、每个连续模拟 400 步（共 80 万次抽样）统计连续相同游程的最大长度，结果全部 ≤4，从未出现 9；把"一次操作对应本地计数器前进 `g` 个单位"这个假设逐个测试 `g=1..15`（同样规模模拟），最大游程仍然全部 ≤4（`g` 越大，下标越可能整体停留在某个新值，不会产生更长的"连续相同"游程），没有任何一个 `g` 值能重现 9 连续相同这个数字。也就是说，**这一确认模型解释了环形相邻游走的整体结构，但没有解释"9 连续相同"这一条具体实测数据**——这不是"需要运行时验证候选数组内容"这类静态分析边界问题（本节的模型完全基于已确认的公开算法和常数，是纯算术推导，不涉及任何运行时才能读到的数据），而是一个已经被精确量化、但尚未找到成因的具体矛盾：要么"9 连续相同"发生时用到的实际本地计数器步进模式比目前假设的更复杂（比如两次触发之间穿插了多种、步进量不固定的操作），要么该次实测记录对应的具体操作序列跟社区转录内容有出入。这一点在第 6 节列为具体待查项，而不是笼统地"存在不确定性"。

## 5. 其他经同一随机流的效果卡

与间谍组织结构相同/相近的其他随机效果卡——护航攻击（CONVOY ATTACK）、反潜巡逻（ASW PATROL）、死神降临（DEATH FROM ABOVE）、加压舱（PRESSURIZED CABIN）——已拆分为独立文档 [OtherRandomCardEffects.md](OtherRandomCardEffects.md)，本文不再收录；它们与间谍组织的代码结构对比、三等分桶推导和待查项见该文档。

## 6. 尚待确认的开放问题（均已定性到静态分析能否解决的边界，或已列为具体量化的待查矛盾）

1. **环形游走的方向（前进/后退）本身是否可预测**：第 3 节已经把"方向"这个问题精确化为"本地计数器步进量 `g` 的函数"——`g` 每增加 3，主导步长精确减少 1（`mod 5`），且 `g` 是 3 的倍数时确定性显著更高（90% 以上）；具体某一次触发对应的 `g` 是多少，取决于两次触发之间实际发生了哪些游戏内操作（跟 Weather.md/[社区实测记录](社区实测记录.md) §1 的操作权重是同一类问题）——只要知道 `g`，方向和幅度都能从上面的表格直接查到，不再是"未知"，而是"需要知道具体对局里的 `g`"这个数据依赖，不是方法论空白。
2. **第一次抽签是否独立于全场累计消费次数**：需要控制"这是全场第几次消费流"这个变量做真实对局对照实验才能确定，静态反编译无法产生这类实测数据。
3. **"9 连续相同"这一条实测数据跟第 2/3/4.3 节已确认模型的具体矛盾**：见第 4.3 节——已经被算清楚，但成因未知的具体矛盾。下一步需要重新核对该次实测对应的具体操作序列（每两次抽卡之间实际做了哪些操作、是否存在步进量不固定的情况），而不是继续用本节的固定步进模型去套。

## 7. 另见

- [README.md](README.md) — 核心随机流对象、播种/重播种机制的完整交叉验证、动作类型与提交流程。
- [Weather.md](Weather.md) — 天气系统的具体实现与同一套机制在另一张卡上的表现。
- [OtherRandomCardEffects.md](OtherRandomCardEffects.md) — 护航攻击、反潜巡逻、死神降临、加压舱等结构相同/相近的卡。
- [社区实测记录](社区实测记录.md) — 社区环形游走/周期实测与术语（非主证据）。
- [ReseedImpact.md](ReseedImpact.md) — 重播种机制如何逐条解释每一个已观察到的现象。
