# 间谍组织（Spy Ring）：一个看似"记忆上一次结果"的伪随机

来源：`Content/Blueprints/Cards/Soviet/Breakthrough/events/card_event_spy_ring.uasset` 的 FModel 反编译（`FModel-Exports/.../card_event_spy_ring.cpp`），结合 IDA/引擎常数推导。方法论呼应 [Weather.md](Weather.md) §3；社区实测数据统一收录于 [社区实测记录](社区实测记录.md) §3，不构成本文主证据，只在第 4 节末尾用于对照。

## 1. 代码实现：只有一行随机数

[`SPY RING`](https://www.kards.com/images/card/v52/en-EN/spy_ring.avif)（苏联 Breakthrough 系列，2 kredits，"Add a random Research card to your hand"，卡图为官方站点链接，非本仓库转载）的整个效果逻辑在 `ExecuteUbergraph_card_event_spy_ring` 里，核心是一次范围 `[0, 4]` 的等概率抽签，再用链式比较映射到 5 张固定的国家研究卡之一：

```cpp
cardFunction->RandomIntFromRangeWithStream(0, 4, /*out*/ randNumber);

if (randNumber == 0) cardToSpawn = "card_event_royal_research";          // 英
else if (randNumber == 1) cardToSpawn = "card_event_us_military_research";     // 美
else if (randNumber == 2) cardToSpawn = "card_event_reich_research";           // 德
else if (randNumber == 3) cardToSpawn = "card_event_imperial_research";        // 日
else if (randNumber == 4) cardToSpawn = "card_event_soviet_military_research"; // 苏

cardFunction->SpawnCardInHandBySide(side, cardToSpawn, cardID, ...);
```

代码本身没有任何"记住上一次抽到谁"的状态——没有额外字段存历史结果，没有基于上一次结果做加权/排除的逻辑。它调用的是 `BP_CardFunctions::RandomIntFromRangeWithStream`，跟天气卡、洗牌等所有"随机选一个"效果共享同一条 `cardsRandomStream`（详见 README.md §1、§3）。单看这张卡的代码，就是最朴素的五选一等概率随机。

## 2. "记忆"从哪来：重播种公式，不是 RNG 算法本身

`RandomIntFromRangeWithStream` → `UKismetMathLibrary::RandomIntegerInRangeFromStream`（一行转发）→ `FRandomStream::RandRange`，最终落到 Unreal 引擎的线性同余生成器（`Engine/Public/Math/RandomStream.h`）：每次取值前先执行 `Seed = Seed * 196314165 + 907633515 (mod 2^32)`，再用变换后 `Seed` 的高 23 位构造 `[0,1)` 浮点数。引擎头文件注释里写明"低位质量很差，不要用取模运算符"——Epic 的实现刻意避开了经典 LCG 低位相关性缺陷，单次调用的统计质量本身没有问题。"看似记忆"的规律不能归因于"对 LCG 输出取模会有相关性"这类笼统说法。

真正产生规律的位置，是 README.md §2.2 的重播种公式：`seed = match_id + action_id * 19390`，真实抓包已确认对局里这个重播种分支是开启的（[evidence/bUseTurnSwitchValidation.md](evidence/bUseTurnSwitchValidation.md)）。间谍组织的消费方式跟天气系统不同：天气三选一是同一次重播种后连续三次变换（`k=1,2,3`），间谍组织是每次触发都各自独立重播种、各自只变换一次（`k=1`）——下面的推导只针对间谍组织这种"单次重播种单次抽样"的结构，不能跟 Weather.md §4.2 的模型混用。

**推导**：设本地计数器从 `action_id` 推进到 `action_id+1`，两次重播种用的种子相差固定的 `19390`。经过 `MutateSeed`（`Seed' = Seed*196314165 + 907633515 mod 2^32`）变换后，两次变换结果的差值同样是一个固定常数，跟 `match_id`、`action_id` 的具体取值无关，只取决于 `196314165` 和 `19390` 这两个已确认常量：

```
Δ_seed = (19390 × 196314165) mod 2^32 = 1,190,635,094
```

`GetFraction()` 只取变换后 `Seed'` 的低 23 位构造 `[0,1)` 浮点数（`mantissa = Seed' & 0x7FFFFF`，再除以 `2^23`）。取低 N 位是模 `2^N` 的环同态，`Δ_seed` 对 23 位小数部分的贡献同样是固定常数：

```
Δ_mantissa = Δ_seed mod 2^23 = 7,841,366   →   Δ_fraction ≈ 0.9348
```

代入 `RandRange(0,4) = floor(GetFraction() × 5)`：`5 × Δ_fraction ≈ 4.674`。本地计数器每前进 1，`RandRange(0,4)` 在"5 等分环"上固定前进约 4.674 格，等价于固定后退约 0.326 格（`mod 5`）。因为每步前进不到 1 格，输出值要么维持不变，要么恰好后退 1 格，不会一步跨过 2 格以上——`Δ_fraction≈0.9348` 是从确认的引擎常数直接算出来的固定值，不依赖 `match_id`，也不依赖对局具体状态。

用 Python 独立模拟复核单次本地计数器步进（`g=1`）的精确离散步长分布（20000 次随机 `match_id`/`action_id` 采样）：`raw_step = (RandRange(0,4)_后 - RandRange(0,4)_前) mod 5` 只取两个值——`0`（67.4%）或 `4`（即 `-1 mod 5`，32.6%），没有出现过其他值。这是把 `Δ_fraction≈0.9348` 代入 `RandRange(0,4)` 离散化规则后的精确算术推论，不是近似估计。

结论：单次本地计数器步进时，原始下标只会停留在原地，或者精确移动到数值上相邻的下一个（`v → v-1 mod 5`）——链式应用后，得到的正是 switch 里那五个数字本来的自然顺序 `0-1-2-3-4-0`（`英-美-德-日-苏`）。这一点完全由已确认的 LCG 常数和离散化规则推出，不依赖运行时数据。

## 3. 离散步长是本地计数器步进量 `g` 的函数

第 2 节只算了 `g=1`。同一套推导对任意步进量 `g` 都成立：把 `Δ_seed(g) = g × 19390 × 196314165 mod 2^32` 代入同样的离散化规则，逐个整数测出每个 `g` 对应的离散步长分布（20000 次随机 `match_id`/`action_id` 采样）：

| `g`（本地计数器步进量） | 主导步长（mod 5） | 出现概率 |
|---|---|---|
| 1 | -1（即 4） | 67.4% |
| 3 | -1（即 4） | 97.7% |
| 6 | +3 | 95.9% |
| 9 | +2 | 93.5% |
| 12 | +1 | 91.5% |
| 15 | 0 | 89.6% |

`g` 每增加 3，主导步长精确减少 1（`mod 5`）——这是第 2 节漂移速率的直接推论；`g` 是 3 的倍数时，主导步长的确定性显著更高（均在 90% 以上）。这张表完全由已确认的 LCG 常数和重播种公式算出。

## 4. 与社区实测的对照

第 1-3 节的推导只用到已确认的 LCG 常数（`196314165`、`19390`）和重播种公式，没有用到任何社区数据。这一节拿第 3 节的表去对照社区独立实测出的规律。

### 4.1 环形相邻游走

社区在 2026-08-24 的视频（BV1WshP6hEHt）里用纯对局内观察复现了一个可验证的规律：把 5 个国家按"美-苏-德-英-日"排成环后，从第二次触发间谍组织起，新结果相对上一次只会是"原地"或"左右相邻"，不会跳到不相邻的国家；第一次结果看起来是纯随机。完整转述见 [社区实测记录](社区实测记录.md) §3.1。

这个环（raw 数值序列 `1,4,2,0,3`）精确满足 `环[i] = (1 + 3×i) mod 5`——固定跳 3，跟第 3 节表格里 `g=6` 那一行（主导步长 `+3`，概率 95.9%）吻合。反过来看，这给出一个具体结论：**社区测试间谍组织时，两次被记录为"连续观察"的抽签之间，本地计数器实际步进的量集中在 6 个单位左右，不是想当然的 1**——跟 ReseedImpact.md/[社区实测记录](社区实测记录.md) §1 记录的"预报单位 3、指令类 1"等社区权重表是同一类问题，只是间谍组织场景下的具体数字是第一次被推出来。

### 4.2 操作间隔与"完整周期"

另一份带原始逐张记录的视频（BV1QyhP62EDQ）按"每两次抽间谍组织之间做 `N` 次激活/取消反制操作"（该视频计数口径下激活、取消各算 1 次，`N=0..8`）分组测试，记录了每组的完整 8 张结果；作者观察到 `N=2/5/8` 三组明显出现循环，两次抽到同一结果之间的操作间隔呈 `3/6/9……`，并据此提出"连续操作 18 次（反制 17 次）能走完一整个循环节、连续刷出 8 张完全相同的研发卡"这个猜想。完整原始数据见 [社区实测记录](社区实测记录.md) §3.2。

把两次抽签之间的本地计数器步进量 `g` 按这份视频的计数口径换算为 `g = N + 1`（`N` 次反制加上间谍组织自身"部署"这一次），代入第 3 节的 `g`-步长表：`N=2,5,8` 对应 `g=3,6,9`——正是该表里离散步长置信度最高的三个点（97.8%、95.7%、93.5%），跟"这三组明显循环"的定性描述精确吻合。

### 4.3 一条具体数据对不上："18 操作→8 张连续相同"

同一支视频、同一套计数口径下的另一条结论——"连续操作 18 次能得到 8 张连续相同结果"——换算成 `g=18` 代入同一张表后对不上：`g=18` 时离散步长的主导值是 `-1`（概率 87%），"保持不变"只有约 13% 的概率；要连续出现 8 张相同结果，需要连续 7 次都落在这约 13% 的分支上，纯随机情形下发生的可能性极低。

同一份原始数据里，"`N=2/5/8` 循环"和"18 操作后 8 连续相同"这两条结论，用同一套已确认的引擎公式和同一个计数换算，不能同时成立——前者已被精确验证吻合，后者是一个精确量化、但成因未知的矛盾。这不属于"需要运行时数据"这类静态分析边界问题，因为第 1-3 节的模型完全基于公开算法和常数、纯算术推导，不涉及任何运行时才能读到的数据。下一步需要重新核对"18 操作、8 张相同"这段具体片段对应的真实操作序列——是否存在跟其余分组不同的、步进量不固定的操作，而不是继续假设整支视频统一遵循同一个 `g=N+1` 换算。

### 4.4 一次逐步记录的复测，矛盾更尖锐

有人按同样的猜想又做了一次复测，逐步记录了每一步操作，包含两次跨回合边界的情形（己方"回合超时结束回合"、对方"结束回合"且无操作）：

```
间谍组织（第1张）→ 17次激活或取消 → 间谍组织（第2张）→ 17次激活或取消 → 间谍组织（第3张）
→ 11次激活或取消 → 回合超时结束回合（己方）→ 敌方结束回合（对方，无操作）→ 6次激活或取消
→ 间谍组织（第4张）→ 17次激活或取消 → 间谍组织（第5张）→ 17次激活或取消 → 间谍组织（第6张）
→ 8次激活或取消 → 回合超时结束回合（己方）→ 敌方结束回合（对方，无操作）→ 9次激活或取消
→ 间谍组织（第7张）→ 17次激活或取消 → 间谍组织（第8张）
```

8 张结果全部是"帝国研发"（日本）。两处跨回合边界的间隔里，反制次数分别拆成了 `11+6=17` 和 `8+9=17`——按"跨回合不额外增加/减少计数"这个假设，全部 7 个间隔都对应同一个 `g = 17（反制）+ 1（间谍组织自身部署）= 18`。

用整数精确运算重新核实（不经过浮点数，避免舍入误差）：50000 次采样里，`g=18` 有 87.1% 的概率是"变化 -1"，只有 12.9% 是"不变"。进一步搜索 30 万个不同的 `match_id`，找连续 6 次按 `g=18` 间隔抽签、全部给出同一个结果的情形——一个都没找到。这不是"发生概率很低但偶尔会发生"，而是在已确认的引擎公式下、大范围搜索都找不到能重现的 `match_id`——比 4.3 节的矛盾证据更完整（有逐步操作日志，不是回忆转述），矛盾也更尖锐。

问题大概率出在"跨回合边界时，反制/己方超时结束回合/对方结束回合各自消耗多少个原始单位"这组假设上，而不是重播种公式本身——本报告对"操作↔原始单位"换算的验证，全部来自不跨回合边界的场景（天气预报连续触发、间谍组织同回合内连续触发）。跨回合边界这个场景目前没有真实抓包数据支持任何具体换算，需要新数据（理想情况下是 mitmproxy 直接记录跨回合边界前后的真实 `action_id`）才能解决，这是一个边界清晰的具体缺口，不是方法论空白。

### 4.5 轮询/websocket 时序：排不上主要成因，但有一个具体的未决子问题

[evidence/CurrentActionId-increment-logic.md](evidence/CurrentActionId-increment-logic.md) 反编译确认，`CurrentActionId`（喂给重播种公式的那个值）只有两条写入路径，都挂在"客户端处理完一次 `/actions` 轮询 HTTP 回包"这个事件上，轮询本身有节流（至少间隔 5 秒，除非被服务端 websocket 推送的 `matchaction` 通知提前打断）。这套机制不足以单独解释 4.3/4.4 的矛盾：正常网络往返是几十到几百毫秒量级，远快于人工连续点击 17 次所需要的实际时间，轮询/websocket 来得及追上，不会出现整串操作都没同步这种量级的滞后。

真正悬而未决的是更具体的一点：触发"绕开节流、立即轮询"的 `matchaction` websocket 通知，客户端收到后不检查发送方是谁就会置位（[evidence/ida-excerpts/BP_KardsSession-matchaction-notification.cpp](evidence/ida-excerpts/BP_KardsSession-matchaction-notification.cpp)）——但这只说明客户端收到后怎么处理，不能说明服务端是否真的会把这条通知也推给动作的提交者本人，还是只推给对手（"通知对方你动了一步"是更常见的实现模式）。如果服务端只通知对手，本机密集操作时就只能依赖 5 秒节流的轮询，而不是每次都被 websocket 提前触发——这跟 4.3/4.4 的矛盾方向相关，但这是服务端行为，反编译客户端看不到，需要真实抓包（记录密集操作时 websocket 消息与轮询请求的真实时间戳）才能确认。

[evidence/live-match-forecast-validation.md](evidence/live-match-forecast-validation.md) 的真实抓包已经确认，客户端和服务端最终结果不存在"猜错、对不上"这种分歧——两场真实对局合计 11 次天气预报，玩家实际收到的结果跟公式算出的预测全部吻合，零例外。这排除了"客户端和服务端本来就可能给出不同结果"的可能：两边用的是同一套确定性回放（双方合并动作日志），计算那一刻双方看到的动作日志状态相同，结果就必然相同。但这批验证数据里每次预报只看一次预览，两步之间是正常人类操作节奏，从未出现过同一张卡被快速连续重复触发预览的密集操作模式——它能确认正常节奏下客户端预览可靠收敛，不能直接回答密集连续触发时中途每一次预览用的 `CurrentActionId` 是否总能跟上，这仍在上面那个未决问题的范围内。

### 4.6 "挂反制"和"取消反制"是同一种提交动作，权重上没有理由不同

社区所称"反制"，游戏内部字段名是 `Gotcha`——一张友方战场单位自带的防御性手牌，激活后能对特定敌方目标效果做一次性抵挡。反编译 `ReceiveActionPlayCardFromHand`（`Content/Blueprints/Logic/BP_OnlineMatch.cpp:4802` 起，完整分支原文见 [evidence/fmodel-excerpts/Gotcha-activate-cancel.cpp](evidence/fmodel-excerpts/Gotcha-activate-cancel.cpp)）确认了一个关键事实：

- "挂反制"和"取消反制"是完全相同的提交动作类型——都是对同一张 Gotcha 手牌重复提交 `XActionPlayCardFromHand`，没有专门的"取消"动作类型。
- 客户端/服务端用这张卡自己的整数字段 `gotchaActivated`（`0`=未激活，`>0`=激活序号）区分这次提交算"激活"还是"取消"：提交时若已经是 `>0`，直接置 `0`（取消）；若是 `0`，扫描同侧全部在场 Gotcha 卡、取当前最大激活序号 `+1` 赋给它（激活，多张反制卡按顺序各自编号）。
- 两条分支走同一段收尾逻辑（`Label_1322`：本机操作才播放 `ToggleGotcha` 动画），没有任何字段或分支表明两者在提交路径上有差别对待。`ToggleGotcha`（`Cards/BP_HandCard.cpp:4628`）是纯本地视觉反馈，只在收到上面这段处理完毕后才被调用，不涉及网络提交或 `CurrentActionId`。

对 4.3/4.4 矛盾的意义：既然"挂"和"取消"在代码里是同一种动作类型，没有一条是本地免费、不提交网络的空操作，那么它们理应各自被 `GetNextAction_Impl`（[evidence/CurrentActionId-increment-logic.md](evidence/CurrentActionId-increment-logic.md)）当作一次普通动作处理，各自让 `CurrentActionId` 加一——这跟社区权重表给"反制"标注的 1 单位（[社区实测记录](社区实测记录.md) §1）吻合，也跟测试者"17 次反制=17 单位"的假设吻合。这条反编译证据没有找到"取消反制不计数"或"激活/取消权重不同"这类能解释矛盾的漏洞——17 次反制切换预期确实消耗 17 个原始单位，"反制切换本身权重算错"这个候选可以基本排除。矛盾焦点因此收窄回跨回合边界权重（4.4 节）和服务端通知范围（4.5 节）这两个仍然悬而未决的候选。

与间谍组织结构相同/相近的其他随机效果卡——护航攻击（CONVOY ATTACK）、反潜巡逻（ASW PATROL）、死神降临（DEATH FROM ABOVE）、加压舱（PRESSURIZED CABIN）——已拆分为独立文档 [OtherRandomCardEffects.md](OtherRandomCardEffects.md)。

## 5. 尚待确认的开放问题

1. **环形游走的方向本身是否可预测**：第 3 节已经把"方向"精确化为本地计数器步进量 `g` 的函数——`g` 每增加 3，主导步长精确减少 1（`mod 5`），且 `g` 是 3 的倍数时确定性显著更高（90% 以上）。具体某一次触发对应的 `g` 是多少，取决于两次触发之间实际发生了哪些游戏内操作（跟 Weather.md/[社区实测记录](社区实测记录.md) §1 的操作权重是同一类问题）——知道 `g` 就能从表格直接查到方向和幅度，这是数据依赖，不是方法论空白。
2. **第一次抽签是否独立于全场累计消费次数**：需要控制"这是全场第几次消费流"这个变量做真实对局对照实验，静态反编译无法产生这类数据。
3. **"17 次激活/取消反制走完一个完整轮回、8 张研发完全相同"跟已确认模型的矛盾**：`N=2/5/8` 循环这条结论已用已确认公式精确验证吻合；"17 次反制+1 次部署（`g=18`）能得到连续相同结果"这条结论，在整数精确运算加 30 万个 `match_id` 的大范围搜索下找不到任何一个能重现"6 次以上连续相同"的情形（4.4 节，含逐步记录的复测数据）——不是发生概率低，是在已确认公式下确认不可复现。4.6 节反编译确认"挂反制"和"取消反制"在提交动作类型上完全相同，"反制切换本身权重算错"这个候选基本排除；4.5 节排查过"轮询/websocket 延迟"这条候选，但正常网络往返速度远快于人工连续点击的节奏，量级上撑不起这个矛盾。剩下两个仍然悬而未决、都需要真实对局抓包才能确认的候选：一是跨回合边界（己方超时结束回合/对方结束回合）各自消耗多少个原始 `CurrentActionId` 单位这组假设本身算错了；二是服务端是否会把 `matchaction` websocket 通知也推给动作提交者本人。
4. **2026-08-25 服务端调整后，本文描述的操纵方式是否仍然有效**：官方[说明](https://www.kards.com/news/forecast-issue-and-fall-2026-tournament-results)只点名了预报机制，但间谍组织走的是同一套 `cardsRandomStream` 重播种机制（README.md §1-§2），已确认同样失效——服务端不再下发 `validate_turn_switches` 字段，重播种分支不再触发，`cardsRandomStream` 变成连续自由推进而非每动作重置。完整技术推导、跟本文环形游走规律的历史关系猜测，见 [RandomnessHotfixMechanism.md](RandomnessHotfixMechanism.md) §5。这不代表第 1-4 节的推导有误——那部分只依赖客户端引擎自带的公开 LCG 算法和已确认常数，跟服务端改了什么无关。

## 6. 另见

- [RandomnessHotfixMechanism.md](RandomnessHotfixMechanism.md) — 2026-08-25 服务端调整的技术原理，含本文环形游走规律与该机制引入时间的历史关系猜测（§5）。
- [README.md](README.md) — 核心随机流对象、播种/重播种机制的完整交叉验证、动作类型与提交流程。
- [Weather.md](Weather.md) — 天气系统的具体实现与同一套机制在另一张卡上的表现。
- [OtherRandomCardEffects.md](OtherRandomCardEffects.md) — 护航攻击、反潜巡逻、死神降临、加压舱等结构相同/相近的卡。
- [社区实测记录](社区实测记录.md) — 社区环形游走/周期实测与术语（非主证据）。
- [ReseedImpact.md](ReseedImpact.md) — 重播种机制如何逐条解释每一个已观察到的现象。
