# 间谍组织（Spy Ring）：一个看似"记忆上一次结果"的伪随机

来源：`Content/Blueprints/Cards/Soviet/Breakthrough/events/card_event_spy_ring.uasset` 的 FModel 反编译（`FModel-Exports/.../card_event_spy_ring.cpp`），加上社区实测视频的归纳结果。方法论呼应 [Weather.md](Weather.md) §3。

## 1. 代码实现：只有一行随机数

`SPY RING`（苏联 Breakthrough 系列，2 kredits，"Add a random Research card to your hand"）的整个效果逻辑在 `ExecuteUbergraph_card_event_spy_ring` 里，核心就是一次范围 `[0, 4]` 的等概率抽签，再用 `switch`-like 的链式比较把结果映射到 5 张固定的国家研究卡之一：

```cpp
cardFunction->RandomIntFromRangeWithStream(0, 4, /*out*/ randNumber);

if (randNumber == 0) cardToSpawn = "card_event_royal_research";          // 英
else if (randNumber == 1) cardToSpawn = "card_event_us_military_research";     // 美
else if (randNumber == 2) cardToSpawn = "card_event_reich_research";           // 德
else if (randNumber == 3) cardToSpawn = "card_event_imperial_research";        // 日
else if (randNumber == 4) cardToSpawn = "card_event_soviet_military_research"; // 苏

cardFunction->SpawnCardInHandBySide(side, cardToSpawn, cardID, ...);
```

代码本身没有任何"记住上一次抽到谁"的状态——没有额外字段存历史结果，没有基于上一次结果做加权/排除的逻辑。它调用的还是 `BP_CardFunctions::RandomIntFromRangeWithStream`，跟天气卡、洗牌、任何"随机选一个"效果共享**同一条** `cardsRandomStream`（详见 README.md §1、§3）。单看这一张卡的代码，会觉得它就是最朴素的"五选一等概率随机"，没有任何值得单独写文档的地方。

## 2. 但社区实测发现了一个"环形规律"

2026-08-24 发布的视频《逻辑推理就能出，4把游戏把间谍组织扒出规律！》（BV1WshP6hEHt）用纯粹的对局内观察（不看反编译代码）复现出了一个具体、可验证的规律，转述如下：

- 把 5 个国家按"美-苏-德-英-日"排成一个环（`美苏德英日`，首尾相接）。
- 同一局内，第一次抽到的国家看起来是"纯随机"（作者没能进一步测出它是否也遵循某种规律，样本不够）。
- **从第二次开始**：每次再触发一张间谍组织，新结果相对上一次的结果，在这个环上只会是"原地"、"向左挪一位"或"向右挪一位"中的一种——不会跳到环上不相邻的国家。方向（左/右）看起来也是随机的，作者同样没能进一步测出其规律。
- 测试方法：双方各自准备多张间谍组织，在同一回合内用"部署间谍组织 → 激活反制 → 取消反制 → 再部署间谍组织"这类不同操作类型反复触发抽卡，按触发顺序把结果记录下来，比对是否始终落在"环上相邻或原地"这个约束内。作者称在 4 局详细记录 + 后续若干局抽查里全部符合。

这个"环形相邻游走"现象，和 Weather.md 里"6K/4K 呈现可预测循环"的观察，是同一类现象在两张不同卡上的表现——**不是间谍组织这张卡有特殊逻辑**（上面第 1 节已经确认代码里没有），而是底层共享随机流本身的行为特征。

## 3. 技术解释：为什么一个"无状态"的等概率随机会呈现出"记忆"

结合 README.md 已经交叉验证的机制，可以给出一个不需要额外假设、自洽的解释：

1. **同一条流，连续消费。** `cardsRandomStream` 是 Unreal 的 `FRandomStream`——一个确定性的、基于内部整数种子做位运算/线性变换的伪随机数发生器（不是每次调用都重新播种，而是每次调用后把内部种子往前推进一步，返回值由推进后的种子算出）。只要 `bUseTurnSwitchValidation` 为 false（见 [evidence/bUseTurnSwitchValidation.md](evidence/bUseTurnSwitchValidation.md)），整场对局这条流只在开局播种一次，之后不管是天气预报的三连抽、间谍组织的单抽，还是其他任何随机效果，全部是**同一个序列上连续的下一个值**，谁调用了 `RandomIntFromRangeWithStream`，谁就把序列往前推进一格。
2. **`% 5` 把一个大范围的连续序列压缩成 5 个桶。** `RandomIntegerInRangeFromStream(stream, 0, 4)` 本质上是把 `FRandomStream` 输出的一个大范围伪随机数映射到 `[0,4]` 这 5 个桶里（等价于对一个更大范围的值取模映射）。**线性同余类 / 位移-异或类伪随机数发生器有一个众所周知的弱点：把连续输出做小模数映射时，相邻输出之间容易出现比"真随机"更强的统计相关性**（低阶输出位的周期性/相关性通常比高阶位差，是 PRNG 设计里的经典陷阱，历史上最著名的例子是 RANDU 这一类 LCG）。间谍组织观察到的"下一次结果只会落在环上相邻位置"，形式上正是"连续两次 `%5` 映射的结果强相关、且相关模式恰好表现为±1"——不需要游戏代码里存在任何"环形状态机"，只需要 `FRandomStream` 在这个特定模数下的序列存在这种相关性即可产生一模一样的可观察现象。这属于待用更多样本量做统计检验（例如相邻差值的分布是否显著偏离均匀分布）来坐实的假设，但它能在**不修改上面第 1 节确认过的代码逻辑**的前提下，完整解释社区观察到的现象，是目前最简洁的解释。
3. **与天气系统的证据形成互证。** Weather.md §3 从"候选池大小 + 固定调用顺序 + 不重播种"的角度解释了天气规律的可预测性；这里从"同一条流的连续小模数映射存在序列相关性"的角度解释间谍组织的环形规律——两者用的是同一份原生随机流基础设施（`cardsRandomStream` / `RandomIntFromRangeWithStream`），只是不同卡牌消费这条流的方式（一次 vs 三次、固定顺序 vs 单次）不同，观察到的"规律形状"也就不同。这两个独立现象共同指向同一个根因，而不是两个互不相关的巧合。

## 4. 环的具体顺序：跟 switch 里数字标号对不上，反而是更强的证据

第 1 节列出的原生 switch 映射，按 `randNumber` 数值顺序排列是：

```
0 → 英 (royal)     1 → 美 (us_military)   2 → 德 (reich)
3 → 日 (imperial)  4 → 苏 (soviet_military)
```

如果直接按这个数字顺序 0→1→2→3→4→0 转一圈，环应该是"英-美-德-日-苏"。但社区实测总结出的环是"美-苏-德-英-日"——**这两个环不是同一个排列**（互相不是简单的旋转或镜像关系）。

这个"对不上"，反而是支持第 3 节技术解释、而不是削弱它的证据：`randNumber` 只是 `RandomIntFromRangeWithStream(0,4)` 返回值本身，switch 语句里 0/1/2/3/4 对应哪个国家，纯粹是策划/程序员写代码时随手赋的标号顺序，跟 `FRandomStream` 内部生成这个整数的算法毫无关系。如果"环形相邻游走"确实来自"同一条 LCG 流连续两次输出、经 `%5` 映射后存在序列相关性"，那么这种相关性只会体现在**流本身产出的原始残数（余数）序列**里，跟策划把哪个余数标为"英国"、哪个标为"美国"没有任何关系——所以社区凭实测归纳出的环，完全可以是、也应该是相对 switch 数字顺序的一个非平凡置换。换句话说：**这个"对不上"恰恰说明规律的根源在随机数生成算法本身，而不是游戏逻辑代码里写了什么"环形状态机"**（第 1 节已经确认代码里根本没有这种状态机）。

## 5. 社区补充测试：操作间隔与"完整周期"猜想

在《更全面的测试——间谍不是随机！》一期的评论区，有观众提出了更细的假设，把间谍组织的规律和"消耗随机流的操作次数"直接挂钩，而不只是"上一次结果是什么"：

- 反复"激活反制→取消反制"（每次算 2 次操作，会各自推进一次 `cardsRandomStream`）测试后发现：**间隔 2 次、5 次、8 次操作后会出现循环**；而"两次真正抽到研发卡的操作"之间，间隔呈 3、6、9……这样按 3 递增的规律。
- 由此提出猜想：连续做 18 次操作（即激活/取消反制 17 次），有可能正好走完一整个循环节，届时连续抽到的 8 张研发卡会完全相同（即整段序列以某个周期折返）。
- 目前这只是社区提出、尚待验证的猜想，还没有实测数据坐实。

这个"操作次数按 3 递增才会撞到同一个结果"的现象，同样可以纳入第 3 节的解释框架：不是每一次"激活/取消反制"都会触发一次真正的抽卡结算（只有部分操作类型才会调用到 `RandomIntFromRangeWithStream`），如果每 3 次可推进流的操作里只有 1 次真正落到"抽一张研发卡"这个节点上，那么"两次真正抽卡之间相隔 3 的倍数"就是操作类型分布的直接结果，不需要额外假设；而"整个大循环节的长度"则取决于 `FRandomStream` 底层算法在 `%5` 映射下的真实周期，这是第 3 节末尾列出的、需要在 IDA 里对 `FRandomStream` 具体实现做逐位分析才能坐实的开放问题，目前只能说社区猜想的"18 次操作一循环"跟本文的因果链方向一致，但精确数值本文尚未能独立验证。

## 6. 其他被社区验证出类似规律的卡

除间谍组织外，社区反馈还在"反潜巡逻"（反潜）、"死亡俯冲"（死降）、"加压驾驶舱"（加压舱）等卡牌上测试出了同样"结果可预测/可复现"的规律。这些卡具体的候选池结构本文尚未逐一反编译核实，但它们的共同点是：效果里都含有"从若干个候选结果中随机选一个"这一步，而它们无一例外都要经过同一个 `RandomIntFromRangeWithStream` 封装、同一条 `cardsRandomStream`——按第 3 节的解释框架，这些卡出现类似规律并不意外，是同一套底层机制在不同"候选数量/候选顺序"的卡面上的又一次重复验证，而不是各自独立的巧合。

## 7. 尚待确认的开放问题

1. 环形游走的"方向随机"本身是否也存在可预测的规律（作者本人也没能测出）——需要更大样本量、并结合 `FRandomStream` 的具体位运算公式做理论推导或统计检验。
2. 第一次抽签是否真的独立于"进入这条流之前，前面已经消耗了多少次"——需要控制"这是全场第几次消费流"这个变量做对照实验，而不仅是控制"同一局内的相对顺序"。
3. 需要在 IDA 里对 `FRandomStream::GetUnsignedInt` / 等价原生实现做逐位分析，确认"连续 `%5` 映射输出强相关"这一假设是否能从算法公式直接推导出来，而不只是靠现象拟合。

## 8. 另见

- [README.md](README.md) — 核心随机流对象、播种/重播种机制的完整交叉验证。
- [Weather.md](Weather.md) — 天气系统的具体实现与同一套机制在另一张卡上的表现。
- [ReseedImpact.md](ReseedImpact.md) — 如果重播种机制实际是开启的，本文第 5 节的猜想会怎么变。
