# 社区实测记录（非本报告主证据）

> 本文档只收录**来自社区的外部观察、第三方工具数据和社区术语**，用于对照主文档（README / Weather / SpyRing / ReseedImpact）中由反编译源码得出的结论。社区数据未经本报告独立复核的部分会在对应条目注明；主文档的结论不依赖本文档中标注为"未独立复核"的数据。

## 1. 社区操作计数方法论

社区在测试预报/间谍组织等效果时，使用一套"操作次数"（社区口径下的"单位"）计数方法，2026-08-25 记录的完整版本如下：

- 统计范围：**两次目标效果之间双方的全部操作都计入**（对手的操作也算；只有"结束回合"例外——己方结束回合计入，对手结束回合不计入）。
- 计数跨回合生效，不是每回合归零。
- 不同类型的操作权重不同：

| 单位数 | 操作类型示例 |
|---|---|
| 1 | 部署、行动（如移动到前线）、指令（打出 order 类卡）、反制、己方结束回合 |
| 2 | 开发（Develop）、观星（社区俗称，见 §4）、毁坏、好高骛远等具体卡牌效果 |
| 3 | 预报单位/预报指令（触发天气预报）、英国太平洋舰队（BPF）、贝克街分队 |

这套方法论与 [ReseedImpact.md](ReseedImpact.md) 中由反编译得到的结论相互印证：真正喂给随机数重播种公式的 `CurrentActionId` 由"回放双方合并动作日志"的状态机推进，不区分动作归属方，因此社区"双方操作都计入"的计数方式与代码行为一致。

## 2. NZ33《天气操作次数计算器》（第三方社区工具）

社区成员 **NZ33** 制作了一个网页小工具《天气操作次数计算器》，把实测规律整理成参数化的图论模型。据社区反馈计算结果准确，但本报告**没有独立复核过该工具的原始测试数据**。

### 2.1 模型结构

每一档天气（2K/4K/6K）是一个大组，大组内分成 3 个小组、按环形排列；每个小组固定装着 3 个具体效果（晴/雨/风各一个）。从当前效果切换到目标效果所需的操作次数，只取决于两者的相对位置：

| 档位 | 环大小 | 同小组代价 | 顺时针跨组代价 | 逆时针跨组代价 |
|---|---|---|---|---|
| 2K | 3 | 15 | 3 | 9 |
| 4K | 3 | 15 | 3 | 9 |
| 6K | 3 | 0 | 1 | 2 |

社区还给出周期数字：2K、4K 天气都是 **18 个（社区单位）操作一循环**，6K 天气是 **3 个（社区单位）操作一循环**；计算器给出的最简操作数可按循环数整数倍叠加（`+18x` 或 `+3x`）。

另有社区记录的现象："指令数大于 3 时 A/B 两档编号同时升一，持续到第 4 次预报"——即以操作次数分段、超过阈值后行为整体切换，本质上是在描述"双方合计、加权后的本地计数器"的某种函数（[ReseedImpact.md](ReseedImpact.md) §4 引用）。

### 2.2 与代码结论的对应

- 6K 循环短（3），日常操作量容易凑成 3 的倍数，表现为"连续两次预报之间不做别的事时看起来不变"；2K/4K 循环长（18），同样的操作量通常凑不成 18 的倍数，更容易表现为"会变"。
- [Weather.md](Weather.md) §4.2 用已确认的 LCG 常数做精确推导后指出：**"2K 与 4K 代价表完全相同"这条经验数据与"同一次重播种后 light/medium/heavy 依次为第 1/2/3 次变换"的模型在数学上不可能同时成立**。该矛盾目前唯一未被排除的可能来源就是 NZ33 工具原始测试数据的精确度，本报告没有渠道独立核实。

### 2.3 社区补充测试（音频转写，视频标题/BV 号未记录）

一份社区测试视频（内容明确针对天气系统）记录了更细颗粒度的观察：

- 两次预报之间插入 **1 次、4 次**操作：三号位（小组 3）的卡保留不变。
- 插入 **0 次（无操作）、3 次、6 次、7 次**操作：也存在"保留部分承接结果"的现象，但重复实验中出现过不一致（6 次的一次重复实验没有保留三号位卡）。
- 插入 **2 次、5 次、8 次**操作：没有观察到保留现象。

1/4、2/5/8 呈现"以 3 为间隔分组、同组内表现一致"的结构，与 §2.1 环长为 3 的模型定性吻合；但该视频未标注测试对应档位（2K/4K/6K）与"三号位"的具体含义，无法把具体操作次数换算成代价表数字。

## 3. 社区对间谍组织/护航攻击的实测

### 3.1 环形游走

2026-08-24 视频《逻辑推理就能出，4把游戏把间谍组织扒出规律！》（BV1WshP6hEHt）用纯对局内观察复现的规律：

- 把 5 个国家按"美-苏-德-英-日"排成一个环（首尾相接）。
- 同一局内第一次抽到的国家看起来是"纯随机"；**从第二次开始**，新结果相对上一次结果只会是"原地"、"向左挪一位"或"向右挪一位"之一，不会跳到环上不相邻的国家。
- 测试方法：双方各自准备多张间谍组织，在同一回合内用"部署间谍组织 → 激活反制 → 取消反制 → 再部署间谍组织"这类操作反复触发抽卡，按触发顺序比对结果。

[SpyRing.md](SpyRing.md) §2 从 LCG 常数推导出的"每步固定后退约 0.326 格、主流游程长度约 3"与这一观察吻合；§4.1 还给出了社区实测的环（美-苏-德-英-日）与代码 switch 标号顺序（英-美-德-日-苏）之间的精确关系——两者由一个简单的模运算公式相连，而非无关的两个排列。

### 3.2 完整周期与"8 张连续相同"

一份原始测试视频（BV1QyhP62EDQ）给出了比早期转述更精确的数据。测试方法：连续使用间谍组织抽 8 张研发卡，每次抽完后先做 `N`（`N=0..8`）次"激活/取消反制"操作再抽下一张——**该视频的计数口径是激活和取消各算 1 次操作**（跟另一支流传更广的视频/转述里"只有激活算、取消不算"的口径不同，两支视频的计数规则本身不一致，不代表游戏机制有歧义，只是两组实验者各自的计数约定不同）。八组结果（国家用中文简称，`N` 是每两张之间的反制操作次数）：

```
N=0: 美德苏英德日苏美
N=1: 日美苏美苏德英日
N=2: 美美英苏日德美美
N=3: 英美美德德日日苏
N=4: 英德苏美日英苏德
N=5: 苏德英苏德英苏德
N=6: 日日日德德德美美
N=7: 英美德日苏英美德
N=8: 美苏美苏美苏美（最后一次操作手滑，该组最后一个结果不计）
```

视频作者的观察：`N=2/5/8` 三组明显出现循环（`N=5` 是 `苏德英` 三国循环，`N=8` 是`美苏`两国循环）；两次抽到同一结果之间的操作次数间隔呈 `3/6/9……`；据此提出"连续操作 18 次（激活/取消反制 17 次）能走完一整个循环节"的猜想，并称该猜想也在本次实验中得到验证，结果是**连续刷出 8 张完全相同的研发卡**（不是本报告此前转述的 9 张——按视频作者本人的计数口径重新记录为准）。

**用已确认的 LCG 公式核对**：间谍组织是单次重播种单次抽样（`k=1`，5 等分环），若把"两次抽签之间的本地计数器步进量"记为 `g`，该视频的计数口径下 `g = N + 1`（`N` 次反制操作，加上抽间谍组织本身这一次"部署"，按 [ReseedImpact.md](ReseedImpact.md) §0 的社区权重表也是 1 单位）。用这个换算对照 [SpyRing.md](SpyRing.md) §3 的完整 `g`-步长表：`N=2,5,8` 对应 `g=3,6,9`——正是该表里离散步长置信度最高的三个点（分别为 97.8%、95.7%、93.5%），跟视频作者"这三组明显循环"的定性观察精确吻合，这是一次独立视频数据对本报告纯代码推导的交叉验证。但"连续操作 18 次能得到连续相同结果"这一条，在同样的 `g=N+1` 换算下对应 `g=18`，而 `g=18` 时离散步长的主导值是 `-1`（概率 87%），"保持不变"只有约 13% 的概率——连续 7 次都保持不变（8 张相同需要 7 次都不变）在这个概率下发生的可能性极低，**这条具体结论与前面"`N=2/5/8` 循环"这条在同一支视频、同一套计数口径下得出的结论，无法同时用已确认的引擎公式解释**。这是一个已经被精确定位、但成因未知的具体矛盾，详见 [SpyRing.md](SpyRing.md) §6。

这条数据后续跟本项目所有者自己一次记录更完整的复测放在一起核对，见 [SpyRing.md](SpyRing.md) §4.3。

### 3.3 CONVOY ATTACK 的 18 次循环

社区用与间谍组织相同的方法测试了护航攻击（CONVOY ATTACK）：以"中间间隔 17 次激活反制 + 使用 CONVOY ATTACK 本身共 18 次操作"验证 **18 是该卡的伪随机循环周期**；循环内 18 次伤害结果没有观察到简单的 0/1/2 循环规律，但 0/1/2 各出现恰好 6 次。

### 3.4 断网重连复现死神降临目标

社区反馈一次针对死神降临（DEATH FROM ABOVE）的测试：在友方回合内断开网络连接，立刻打出死神降临，观察到敌方某个单位被消灭；随后重新加载客户端、恢复网络，对局状态回到"打出死神降临之前"，再次打出死神降临，随机目标与断网时的第一次完全相同。

[evidence/CurrentActionId-increment-logic.md](evidence/CurrentActionId-increment-logic.md) 反编译确认的机制可以解释这个现象：`CurrentActionId`（喂给重播种公式的本地计数器）只有两条已知写入路径，两条都挂在"处理一次 `/actions` 轮询 HTTP 回包"这个事件上；而触发"绕开 5 秒节流、立即轮询"的机制，是服务端通过 websocket 推送的 `matchaction` 通知——断网状态下这条通知根本收不到、轮询请求本身也发不出去，`CurrentActionId` 因而被冻结在断网前最后一次确认的值上不动。断网后立刻打出死神降临，用的正是这个冻结住的 `CurrentActionId` 算出的随机目标；重新加载客户端、恢复网络后，只要对局重放到的确认动作历史与断网前完全一致（没有新的确认动作被处理），`CurrentActionId` 就会回放到同一个值，再次打出同一张卡自然算出同一个目标——这不是"客户端本地存了随机结果"，而是"喂给确定性公式的种子分量恢复成了同一个数"这套已确认机制的又一次直接体现。

### 3.5 其他卡的社区反馈

社区反馈在反潜巡逻（ASW PATROL）、死神降临（DEATH FROM ABOVE）、加压舱（PRESSURIZED CABIN）等卡上也测出类似"结果可预测/可复现"的规律。三张卡均已由 [OtherRandomCardEffects.md](OtherRandomCardEffects.md) §2 反编译核实结构一致（走 `GetRandomCard` 动态随机），社区反馈与代码结论方向一致。

## 4. 社区术语与简称（仅用于对照，正式文档使用官方译名）

| 社区说法 | 官方卡名（en-EN / zh-Hans） | 说明 |
|---|---|---|
| 死降 | DEATH FROM ABOVE / 死神降临 | 社区简称，非正式名称 |
| 反潜 | ASW PATROL / 反潜巡逻 | 社区简称，非正式名称；"反潜"只是对 Anti-Submarine Warfare 的直译缩写 |
| 船队袭击 | CONVOY ATTACK / 护航攻击 | 社区曾用译名，官方中文名为"护航攻击" |
| 观星 | 泛指"看牌库顶 3 选 1"类效果，如 SPOILS OF WAR / 战利品 | 社区俗称；"观星车"指带此类效果的三号坦克 H 型等 |
| 毁坏 | SABOTAGE / 毁坏 | 苏联 7K 指令：从敌方 3 张手牌中选择 1 张弃掉并加入副本 |
| 好高骛远 | GRAND PLANS / 好高骛远 | 日本 1K 指令（官方中文名即此） |

正式文档一律使用官方中文名（见 [README.md](README.md) 与各主文档），本表仅用于阅读社区资料时对照。

## 5. 与主文档的关系

- 主文档只保留可由反编译源码/引擎常数独立推导的结论。
- 本文档中的社区数据用于交叉验证或作为待查矛盾的来源；凡是主文档标注"社区数据、未经独立复核"的条目，均以本文档为索引。
- 官方 2026-08-23《关于秋季锦标赛预报机制的说明》属于官方公开声明，不属于社区推测，相关内容保留在 [README.md](README.md) §6 / [Weather.md](Weather.md) §3。
